---
import GoogleTag from "../components/GoogleTag.astro";
import SiteHeader from "../components/SiteHeader.astro";
import SiteScripts from "../components/SiteScripts.astro";
import "../styles/global.css";

const baseUrl = import.meta.env.BASE_URL;
const spriteUrl = `${baseUrl}images/reference/restoo-sprites.svg`;
---

<!doctype html>
<html lang="es">
  <head>
    <GoogleTag />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Reserva catas y comidas en WineNotFound con disponibilidad de lunes a domingo, excepto miércoles."
    />
    <meta name="theme-color" content="#2b1018" />
    <link rel="icon" href={`${baseUrl}favicon.svg`} type="image/svg+xml" />
    <title>Reservar | WineNotFound</title>
  </head>
  <body class="booking-body" style={`--booking-bg: url("${baseUrl}images/reference/background.jpg")`}>
    <SiteHeader />

    <main>
      <section
        class="booking-hero"
        aria-labelledby="booking-title"
        style={`--booking-bg: url("${baseUrl}images/reference/background.jpg")`}
      >
        <div class="booking-card booking-card-wide" data-booking-card data-active-panel="details">
          <form class="booking-form" data-booking-form>
            <section class="booking-panel is-active" data-booking-panel="details">
              <div class="booking-heading">
                <h1 id="booking-title">Reserve en WineNotFound.</h1>
                <p>Ctra. Nacional 232, Briones</p>
              </div>

              <div class="booking-stepper" data-quantity-control>
                <label for="adults"><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#guest`}></use></svg> Adultos</label>
                <div class="stepper-row">
                  <button type="button" data-step="-1" aria-label="Restar adulto">-</button>
                  <input id="adults" name="adults" type="number" value="2" min="1" max="12" readonly />
                  <button type="button" data-step="1" aria-label="Sumar adulto">+</button>
                </div>
              </div>

              <div class="booking-stepper" data-quantity-control>
                <label for="children">
                  <svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#guest`}></use></svg> Niños
                  <small>Hasta 12 años</small>
                </label>
                <div class="stepper-row">
                  <button type="button" data-step="-1" aria-label="Restar niño">-</button>
                  <input id="children" name="children" type="number" value="0" min="0" max="8" readonly />
                  <button type="button" data-step="1" aria-label="Sumar niño">+</button>
                </div>
              </div>

              <fieldset class="booking-date-options">
                <legend><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#date`}></use></svg> Fecha</legend>
                <div class="booking-date-grid" data-booking-date-options></div>
              </fieldset>

              <fieldset class="booking-options" data-booking-type-options>
                <legend><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#time`}></use></svg> Reserva para</legend>
                <label>
                  <input type="radio" name="slot" value="cata" data-booking-type />
                  <span>CATA<small>10:30 - 17:00</small></span>
                </label>
                <label>
                  <input type="radio" name="slot" value="comida" data-booking-type checked />
                  <span>COMIDA<small>13:30 - 15:30</small></span>
                </label>
              </fieldset>

              <button class="button button-primary booking-main-button" type="button" data-booking-continue>CONTINUAR</button>

              <div class="booking-actions">
                <a href={`${baseUrl}tienda/#suscripcion`}>VER EXPERIENCIAS PARA REGALAR</a>
                <a href={`${baseUrl}tienda/#suscripcion`}>CANJEAR TARJETA REGALO O CÓDIGO</a>
              </div>

              <p class="booking-phone">Llámenos al <a href="tel:+34941324254">941 32 42 54</a> si tiene cualquier duda.</p>
            </section>

            <section class="booking-panel" data-booking-panel="times" hidden>
              <p class="booking-summary-line" data-booking-summary></p>
              <h2>Elija un horario.</h2>
              <div class="booking-time-grid booking-time-grid-large" data-booking-times></div>
              <button class="button button-outline booking-back-button" type="button" data-booking-back>RETROCEDER</button>
              <p class="booking-phone">Llámenos al <a href="tel:+34941324254">941 32 42 54</a> si tiene cualquier duda.</p>
            </section>

            <section class="booking-panel" data-booking-panel="guest" hidden>
              <h2>Ingrese sus datos personales.</h2>
              <div class="booking-confirm-summary">
                <span><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#date`}></use></svg><strong data-confirm-date></strong></span>
                <span><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#time`}></use></svg><strong data-confirm-time></strong></span>
                <span><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#guest`}></use></svg><strong data-confirm-people></strong></span>
                <button class="checkout-back" type="button" data-booking-modify>MODIFICAR</button>
              </div>

              <div class="booking-guest-fields">
                <input type="text" name="name" autocomplete="name" placeholder="Nombre completo" required />
                <input type="email" name="email" autocomplete="email" placeholder="Email" required />
                <div class="phone-row">
                  <span class="phone-prefix"><span class="flag-spain" aria-hidden="true"></span> +34</span>
                  <input type="tel" name="phone" autocomplete="tel" placeholder="Teléfono" required />
                </div>
                <input
                  type="text"
                  name="postal"
                  inputmode="numeric"
                  autocomplete="postal-code"
                  placeholder="Código postal"
                  required
                />
                <fieldset class="allergy-toggle">
                  <legend>¿Alguna persona tiene alergias y/o intolerancias?</legend>
                  <div>
                    <label><input type="radio" name="allergies" value="no" checked /> NO</label>
                    <label><input type="radio" name="allergies" value="si" /> SÍ</label>
                  </div>
                </fieldset>
                <button class="special-requests-button" type="button">AÑADIR PETICIONES ESPECIALES</button>
                <label class="booking-checkbox">
                  <input type="checkbox" name="terms" required />
                  <span>Acepto las <a href={`${baseUrl}aviso-legal/`}>Condiciones de Uso</a> y <a href={`${baseUrl}aviso-legal/`}>Política de Privacidad</a>.</span>
                </label>
                <label class="booking-checkbox">
                  <input type="checkbox" name="newsletter" />
                  Quiero recibir información comercial por email y/o SMS.
                </label>
                <label class="booking-checkbox">
                  <input type="checkbox" name="reminder" />
                  Recordarme para la próxima reserva.
                </label>
              </div>

              <button class="button button-primary booking-confirm-button" type="submit">CONFIRMAR RESERVA</button>
              <p class="booking-phone">Llámenos al <a href="tel:+34941324254">941 32 42 54</a> si tiene cualquier duda.</p>
              <p class="booking-legal-note">
                Recogemos datos anónimos, sin posibilidad de identificación personal, para cumplir con
                las obligaciones de información del restaurante respecto a alérgenos, y/o las posibles
                necesidades de accesibilidad. Por tanto, sobre esta información, no es de aplicación la
                normativa europea de protección de datos personales (Artículo 2, Artículo 4, Apartados 1 y
                5 y Considerandos 14, 15, 26, 27, 29 y 30 del RGPD).
              </p>
            </section>

            <section class="booking-panel booking-success-panel" data-booking-panel="success" hidden>
              <header class="booking-success-topbar">
                <span>WineNotFound</span>
                <span><span aria-hidden="true">◎</span> ES</span>
              </header>

              <div class="booking-success-body">
                <h2 class="booking-success-title">
                  <span class="success-check" aria-hidden="true"></span>
                  <span><strong>Gracias,</strong> su reserva está confirmada.</span>
                </h2>

                <div class="booking-success-summary">
                  <span><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#date`}></use></svg><strong data-success-date></strong></span>
                  <span><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#time`}></use></svg><strong data-success-time></strong></span>
                  <span><svg class="booking-icon-svg" aria-hidden="true"><use href={`${spriteUrl}#guest`}></use></svg><strong data-success-people></strong></span>
                </div>

                <aside class="booking-gift-box">
                  <h3>¿Necesita hacer un regalo especial?</h3>
                  <p>Regale una experiencia única en WineNotFound.</p>
                  <a href={`${baseUrl}tienda/#suscripcion`}>VER EXPERIENCIAS PARA REGALAR</a>
                </aside>

                <div class="booking-success-actions">
                  <button type="button"><span class="success-action-icon success-action-bell" aria-hidden="true"></span>AVISAR A MIS ACOMPAÑANTES</button>
                  <a href={`${baseUrl}#menu`}><span class="success-action-icon success-action-menu" aria-hidden="true"></span>VER CARTA Y MENÚ</a>
                  <a href="https://www.google.com/maps/search/?api=1&query=WineNotFound%20Ctra.%20Nacional%20232%20Briones"><span class="success-action-icon success-action-pin" aria-hidden="true"></span>CÓMO LLEGAR</a>
                  <button type="button"><span class="success-action-icon success-action-calendar" aria-hidden="true"></span>AÑADIR A MI CALENDARIO</button>
                  <button type="button"><span class="success-action-icon success-action-contact" aria-hidden="true"></span>AÑADIR WINENOTFOUND<br />A MIS CONTACTOS</button>
                  <button class="booking-success-modify" type="button" data-success-modify><span class="success-action-icon success-action-edit" aria-hidden="true"></span>MODIFICAR RESERVA</button>
                </div>
              </div>
            </section>
            <p class="form-status" data-booking-status role="status" aria-live="polite"></p>
          </form>
        </div>
      </section>
    </main>
    <SiteScripts />
  </body>
</html>
